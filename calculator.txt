<!DOCTYPE html>
<html>

<body>

<?php

function calculadora($numA, $numB, $op) {
    if ($op == "somar") {
        return $numA + $numB;
    } elseif ($op == "subtrair") {
        return $numA - $numB;
    } elseif ($op == "dividir") {
        return $numA / $numB;
    } elseif ($op == "multiplicar") {
        return $numA * $numB;
    } else {
        return "ESSE OPERADOR NÃO EXISTE!";
    }
}

echo calculadora(1, 2, "somar");

?>

</body>

</html>